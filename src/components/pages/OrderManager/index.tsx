import { Box } from '@chakra-ui/react';
import ManagerTemplate from '../../templates/ManagerTemplate';

const OrderManager = () => {
    return (
        <ManagerTemplate>
            <Box>order manager</Box>
        </ManagerTemplate>
    );
};

export default OrderManager;
